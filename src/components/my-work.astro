---
import type { SiteContent } from '../utils/content'

interface Props {
  myWork: SiteContent['myWork']
}

const { myWork } = Astro.props

const projectBadge = (isClient?: boolean) => (isClient ? myWork.projectBadge.client : myWork.projectBadge.personal)
const projectBadgeColor = (isClient?: boolean) =>
  isClient
    ? 'border border-[#0CA000]/40 bg-white/90 text-[#0CA000] dark:border-[#06C300]/40 dark:bg-slate-900/80 dark:text-[#06C300]'
    : 'border border-[#31633D]/40 bg-white/90 text-[#31633D] dark:border-[#06C300]/30 dark:bg-slate-900/80 dark:text-[#06C300]'

const sortedProjects = [...myWork.projects].sort((a, b) => Number(b.job ?? false) - Number(a.job ?? false))
---

<section id="my-work" class="w-full space-y-12">
  <header class="space-y-3">
    <h2 class="text-3xl font-extrabold leading-tight text-slate-900 dark:text-white">{myWork.title}</h2>
    <p class="max-w-3xl text-sm text-slate-600 dark:text-slate-300 md:text-base">{myWork.description}</p>
  </header>
  <article class="space-y-6">
    <header class="flex flex-col gap-2">
      <h3 class="text-2xl font-semibold text-slate-900 dark:text-white">{myWork.projectsTitle}</h3>
      <p class="max-w-3xl text-sm text-slate-600 dark:text-slate-300 md:text-base">{myWork.projectsDescription}</p>
    </header>
    <div class="grid gap-6">
      {sortedProjects.map((project) => (
        <article class="group grid overflow-hidden rounded-[28px] border border-slate-200 bg-white/85 shadow-sm shadow-slate-400/20 transition hover:-translate-y-1 hover:shadow-xl hover:shadow-slate-400/40 dark:border-slate-700 dark:bg-slate-900/60 dark:shadow-black/30 md:grid-cols-[minmax(0,1.1fr)_minmax(0,1fr)]">
          <div class="relative h-full">
            <img
              src={project.image}
              alt={project.name}
              width="640"
              height="480"
              class="h-full w-full object-cover transition duration-300 group-hover:scale-[1.03]"
              loading="lazy"
            />
            <span class={`absolute left-4 top-4 rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-[0.3em] shadow-sm shadow-black/20 ${projectBadgeColor(project.job)}`}>
              {projectBadge(project.job)}
            </span>
          </div>
          <div class="flex flex-col gap-4 p-6">
            <div class="flex flex-col gap-2">
              <h4 class="text-xl font-semibold text-slate-900 dark:text-white">{project.name}</h4>
              <p class="text-sm text-slate-600 dark:text-slate-300 md:text-base">{project.description}</p>
            </div>
            <ul class="mt-2 grid gap-2 text-sm text-slate-600 dark:text-slate-300">
              {project.highlights.map((highlight) => (
                <li class="flex items-start gap-2">
                  <span aria-hidden="true" class="mt-1 inline-flex h-2 w-2 shrink-0 rounded-full bg-[#0CA000] dark:bg-[#06C300]"></span>
                  <span>{highlight}</span>
                </li>
              ))}
            </ul>
            <div class="flex flex-wrap gap-2 text-xs text-slate-600 dark:text-slate-300">
              {project.skills.map((skill) => (
                <span class="rounded-full bg-slate-100 px-3 py-1 dark:bg-slate-800">{skill}</span>
              ))}
            </div>
            <a
              href={project.demo}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-2 text-sm font-semibold text-[#31633D] transition hover:text-[#0CA000] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#06C300] dark:text-[#06C300] dark:hover:text-white"
            >
              {myWork.projectCta}
              <span aria-hidden="true">↗</span>
            </a>
          </div>
        </article>
      ))}
    </div>
  </article>

  <article class="space-y-6">
    <header class="flex flex-col gap-2">
      <h3 class="text-2xl font-semibold text-slate-900 dark:text-white">{myWork.skillsTitle}</h3>
      <p class="text-sm text-slate-600 dark:text-slate-300 md:text-base">{myWork.skillsDescription}</p>
    </header>
    <div class="grid gap-5 md:grid-cols-2">
      {myWork.skills.map((group) => (
        <div class="rounded-3xl border border-slate-200 bg-white/80 p-5 shadow-sm shadow-slate-400/20 dark:border-slate-700 dark:bg-slate-900/60 dark:shadow-black/30">
          <h4 class="text-lg font-semibold text-slate-900 dark:text-white">{group.title}</h4>
          <ul class="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">
            {group.items.map((skill) => (
              <li class="flex items-center gap-3 rounded-2xl bg-white/70 p-3 text-sm font-medium text-slate-600 shadow-sm shadow-slate-200/60 transition hover:-translate-y-1 hover:shadow-lg hover:shadow-slate-300/40 dark:bg-slate-900/60 dark:text-slate-200 dark:shadow-black/30">
                <img src={`/skills/${skill.slug}.svg`} alt="" aria-hidden="true" class="h-7 w-7" />
                <span>{skill.label}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </article>

  <article class="space-y-6">
    <header class="flex flex-col gap-2">
      <h3 class="text-2xl font-semibold text-slate-900 dark:text-white">{myWork.servicesTitle}</h3>
      <p class="text-sm text-slate-600 dark:text-slate-300 md:text-base">{myWork.servicesDescription}</p>
    </header>
    <div class="grid gap-6 md:grid-cols-3">
      {myWork.serviceKits.map((kit) => (
        <div class="flex h-full flex-col gap-4 rounded-3xl border border-dashed border-[#31633D]/40 bg-white/75 p-5 text-sm text-slate-600 shadow-sm shadow-[#31633D]/15 transition hover:-translate-y-1 hover:shadow-lg hover:shadow-[#31633D]/20 dark:border-[#06C300]/40 dark:bg-slate-900/60 dark:text-slate-300 dark:shadow-[#06C300]/15">
          <div>
            <h4 class="text-lg font-semibold text-slate-900 dark:text-white">{kit.title}</h4>
            <p class="mt-3">{kit.description}</p>
          </div>
          <ul class="space-y-2 text-sm text-slate-600 dark:text-slate-300">
            {kit.deliverables.map((item) => (
              <li class="flex items-start gap-2 rounded-2xl bg-white/70 px-3 py-2 dark:bg-slate-900/70">
                <span aria-hidden="true" class="mt-[6px] inline-flex h-1.5 w-1.5 shrink-0 rounded-full bg-[#0CA000] dark:bg-[#06C300]"></span>
                <span class="text-sm font-medium text-slate-600 dark:text-slate-200">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </article>

  <article class="space-y-6">
    <header class="flex flex-col gap-2">
      <h3 class="text-2xl font-semibold text-slate-900 dark:text-white">{myWork.certificationsTitle}</h3>
      <p class="text-sm text-slate-600 dark:text-slate-300 md:text-base">{myWork.certificationsDescription}</p>
    </header>
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {myWork.certifications.map((certification) => (
        <a
          href={certification.url}
          target="_blank"
          rel="noopener noreferrer"
          class="group flex flex-col overflow-hidden rounded-3xl border border-slate-200 bg-white/80 p-5 text-left shadow-sm shadow-slate-400/20 transition hover:-translate-y-1 hover:shadow-lg hover:shadow-slate-400/40 dark:border-slate-700 dark:bg-slate-900/60 dark:shadow-black/30"
        >
          <img
            src={certification.image}
            alt={certification.name}
            width="480"
            height="320"
            class="h-40 w-full rounded-2xl bg-white/80 object-contain p-4 transition duration-300 group-hover:scale-[1.02]"
            loading="lazy"
          />
          <div class="mt-5 flex flex-col gap-2">
            <span class="text-lg font-semibold text-slate-900 dark:text-white">{certification.name}</span>
            <span class="text-sm font-medium text-slate-500 dark:text-slate-300">{certification.organization} · {certification.date}</span>
            {certification.description && (
              <p class="text-sm text-slate-600 dark:text-slate-300">{certification.description}</p>
            )}
          </div>
        </a>
      ))}
    </div>
  </article>
</section>
