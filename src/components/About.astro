---
import type { SiteContent } from '../utils/content'

interface Props {
  about: SiteContent['about']
}

const { about } = Astro.props

---

<section id="about" class="w-full space-y-10">
  <div class="grid gap-6 md:grid-cols-[minmax(0,1.2fr)_minmax(0,1fr)]">
    <article class="space-y-5 text-base text-slate-600 dark:text-slate-300 md:text-lg">
      <h2 class="text-3xl font-extrabold leading-tight text-slate-900 dark:text-white">{about.title}</h2>
      {about.paragraphs.map((paragraph) => (
        <p>{paragraph}</p>
      ))}
      <dl class="grid gap-4 rounded-3xl border border-slate-200 bg-white/70 p-5 text-sm text-slate-600 shadow-sm shadow-slate-400/20 dark:border-slate-700 dark:bg-slate-900/60 dark:text-slate-300 dark:shadow-black/30">
        {about.highlights.map((highlight) => (
          <div class="flex items-start gap-3">
            <dt class="mt-1 h-2.5 w-2.5 rounded-full bg-[#0CA000] dark:bg-[#06C300]"></dt>
            <dd>{highlight}</dd>
          </div>
        ))}
      </dl>
    </article>

    <article class="grid gap-4">
      {about.focusAreas.map((area) => (
        <div class="rounded-3xl border border-slate-200 bg-white/80 p-5 shadow-sm shadow-slate-400/20 transition hover:-translate-y-1 hover:shadow-lg hover:shadow-slate-400/30 dark:border-slate-700 dark:bg-slate-900/60 dark:shadow-black/30">
          <h3 class="text-xl font-semibold text-slate-900 dark:text-white">{area.title}</h3>
          <p class="mt-3 text-sm text-slate-600 dark:text-slate-300">{area.description}</p>
        </div>
      ))}
    </article>
  </div>

  <section class="rounded-[32px] border border-slate-200 bg-white/80 p-6 shadow-sm shadow-slate-400/20 dark:border-slate-700 dark:bg-slate-900/60 dark:shadow-black/30 md:p-10">
    <h3 class="text-2xl font-semibold text-slate-900 dark:text-white">{about.workflowTitle}</h3>
    <p class="mt-3 max-w-3xl text-sm text-slate-600 dark:text-slate-300 md:text-base">{about.workflowDescription}</p>
    <ol class="mt-8 grid gap-6 md:grid-cols-3">
      {about.workflowSteps.map((step) => (
        <li class="flex h-full flex-col gap-3 rounded-3xl border border-dashed border-[#31633D]/40 bg-white/70 p-5 text-slate-600 shadow-sm shadow-[#31633D]/15 dark:border-[#06C300]/40 dark:bg-slate-900/60 dark:text-slate-300 dark:shadow-[#06C300]/15">
          <span class="text-xs font-semibold uppercase tracking-[0.35em] text-[#31633D] dark:text-[#06C300]">{step.step}</span>
          <h4 class="text-lg font-semibold text-slate-900 dark:text-white">{step.title}</h4>
          <p class="text-sm">{step.description}</p>
        </li>
      ))}
    </ol>
  </section>
</section>
